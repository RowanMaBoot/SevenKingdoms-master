decisions = {

	commit_to_training = {
		potential = {
			age = 16
			prisoner = no
			NOT = { 
				has_character_flag = already_trained 
				trait = wounded
				trait = incapable
				trait = maimed
				trait = infirm
				trait = dwarf
				trait = sloth
			}
		}
		allow = {
			prisoner = no
		}
		effect = {
			hidden_tooltip = {
				character_event = { id = dueling.1 }
				set_character_flag = already_trained
			}
		}
	}
}