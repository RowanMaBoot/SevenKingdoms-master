decisions = {

	commit_to_training = {
		potential = {
			age = 16
			prisoner = no
			NOT = { 
				has_character_flag = already_trained 
				trait = wounded
				trait = incapable
				trait = maimed
				trait = infirm
				trait = dwarf
				trait = slothful
			}
		}
		allow = {
			prisoner = no
		}
		effect = {
			hidden_tooltip = {
				character_event = { id = dueling.1 }
				set_character_flag = already_trained
				mean_time_to_happen = {
					months = 30
					clr_character_flag = already_trained
				}
			}
		}
	}
}