#The beginning of dragon events.
namespace = dragon

#Hidden Dragon events
character_event = {
	id = dragon.1
	
	is_triggered_only = yes
	hide_window = yes

	trigger = {
		trait = dragon
	}
	
	immediate = {
		if = {
			limit = { is_ill = yes }
			random = {
				chance = 50
				modifier = {
					factor = 1.5
					trait = strong
				}
				heal_minor = yes
			}
		}
	}
}
