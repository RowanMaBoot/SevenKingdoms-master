targetted_decisions = {
	found_gnostics= {
		ai_target_filter = self
		filter = self

		potential = {
			age = 16
			trait = mystic		
			ai = no
		}
	
		allow = {	
			age = 16
			trait = mystic		
			ai = no
		}
	
		effect = {
			join_society = gnostics
			set_society_grandmaster = yes
			z_magic_power_sequential_addition = yes
			z_magic_power_sequential_addition = yes
			z_magic_power_sequential_addition = yes
			character_event = { id = gnostics.3000 }
		}
	}
}